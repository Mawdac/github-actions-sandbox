{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "test-test": "echo 'tests'",
    "build": "next build",
    "build-test": "echo 'build'",
    "start": "next start",
    "npm-commit": "git commit --edit -m\"$(git log --format=%B --reverse HEAD..HEAD@{1})\"",
    "preversion": "npm run test-test",
    "version": "npm run build-test",
    "postversion": "git reset --soft HEAD~3 && npm run npm-commit && git push --follow-tags -f"
  },
  "dependencies": {
    "next": "latest",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "version": "3.0.2"
}
