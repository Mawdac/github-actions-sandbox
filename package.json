{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "test": "echo 'tests'",
    "build": "next build",
    "start": "next start",
    "npm-commit": "git commit -m\"$(git log --format=%B --reverse HEAD..HEAD@{1})\"",
    "preversion": "npm test",
    "postversion": "git reset --soft HEAD~2 && npm run npm-commit && git push --follow-tags -f"
  },
  "dependencies": {
    "next": "latest",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "version": "4.0.0"
}
